/**
 * §16.1 personalization-consent copy. VERBATIM — the exact string (em dash and
 * punctuation included) shown on the first-login consent card. Do not reword.
 */
export const PERSONALIZATION_CONSENT_COPY =
  'Allow InTown to learn from how you plan and travel — this only improves your experience. We never sell your data and never send spam.';

/**
 * Version of the consent copy/policy recorded with each consent decision
 * (§16.1). Bumped whenever the copy above changes so learning can attribute
 * signals to the policy the user agreed to.
 */
export const CONSENT_POLICY_VERSION = '2026-07-01';
