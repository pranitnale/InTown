{
  "name": "intown",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node scripts/boundary-guard.mjs && node --test \"scripts/*.test.mjs\" && pnpm -r run test && bash scripts/python-tests.sh"
  }
}
