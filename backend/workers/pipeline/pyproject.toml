[project]
name = "intown-pipeline"
version = "0.0.0"
description = "InTown City Brain build pipeline (§5.2): POI ingestion, research, enrichment workers."
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
# Populated by later pipeline phases (Overpass/HTTP clients, LLM SDKs, pydantic
# models generated from contracts/python). Kept empty in P00.
dev = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["pipeline"]
